# Lists datatype in python 

mylist = []
#we can write lists like this variable = [squere bracket within this we can write list]
mylist.append(1)
mylist.append(2)

for item in mylist:
    print(item)
    
# Other hand we can declare the list like this 

mylist1 = [1,2,3,4,5,6]     # these are the items in list
mylist1.append(7)     # adding another item to list using append (append means add to last )
print(mylist1)